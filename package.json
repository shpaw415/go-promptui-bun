{
  "name": "go-promptui-bun",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build:windows": "CGO_ENABLED=1 go build -buildmode=c-shared -o dist/lib/libprompt.so src/main.go",
    "start": "bun index.ts"
  }
}
